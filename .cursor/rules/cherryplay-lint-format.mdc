---
description: Lint, format and completion checks for CherryPlay (CherryPlayWeb, CherryPlayList, CherryPlayComponents)
alwaysApply: true
---

# CherryPlay Lint, Format and Completion

После любых изменений в файлах TypeScript/JavaScript/React (.ts, .tsx, .js, .jsx) в проектах CherryPlayWeb, CherryPlayList или CherryPlayComponents соблюдай следующие правила.

## Автоматический запуск линтера

1. Если изменены файлы в `CherryPlayWeb/src/` — запусти `cd CherryPlayWeb && npm run lint:fix`
2. Если изменены файлы в `CherryPlayList/src/` — запусти `cd CherryPlayList && npm run lint:fix`
3. Если изменены файлы в `CherryPlayComponents/src/` — запусти `cd CherryPlayComponents && npm run lint:fix`
4. Если изменены файлы в нескольких проектах — запусти линтер для каждого из них

## Форматирование кода

Все изменения должны соответствовать правилам ESLint и Prettier проекта. При необходимости автоматически исправляй форматирование через `lint:fix`.

## Проверка перед завершением задачи

Перед завершением задачи убедись, что:

- Линтер пройден без ошибок
- Код соответствует стандартам проекта
- Все изменения протестированы
