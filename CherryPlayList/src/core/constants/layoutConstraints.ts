/** Maximum nesting depth for split containers (0 = single workspace leaf). */
export const MAX_LAYOUT_DEPTH = 6;

/** Maximum workspace zones per split container. */
export const MAX_ZONES_PER_CONTAINER = 10;
